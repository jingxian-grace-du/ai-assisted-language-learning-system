# Validators

This directory is reserved for deterministic and semantic validation components.

Validators should return machine-readable evidence and stable failure codes. A generator must not be able to declare its own output valid without the required independent checks.
