# Exporters

This directory is reserved for serialising validated notebook units into canonical interchange formats and Anki-compatible records.

Exporters must not bypass failed gates or treat temporary import copies as canonical data.
