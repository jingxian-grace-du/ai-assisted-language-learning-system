# Tests

Tests will cover schemas, validators, pipeline invariants, error-class regressions, and privacy controls.

Only wholly synthetic content may be stored in this repository. Private evaluation data and approved learning standards must remain outside the repository.

Run the current foundation suite with:

```text
python -m unittest discover -s tests -v
```
